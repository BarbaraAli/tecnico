{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ficha list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecha</th>
                <th>Solicitado</th>
                <th>Dependencia</th>
                <th>Observaciones</th>
                <th>Realizadopor</th>
                <th>Firmadopor</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ficha_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.solicitado }}</td>
                <td>{{ entity.dependencia }}</td>
                <td>{{ entity.observaciones }}</td>
                <td>{{ entity.realizadopor }}</td>
                <td>{{ entity.firmadopor }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ficha_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ficha_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ficha_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
